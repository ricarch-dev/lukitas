# Tasks: Continue USDT Monetary-Unit Support

## Review Workload Forecast

| Field | Value |
|---|---|
| Estimated authored changed lines (remaining work only) | Approximately 2,400–3,800 additions + deletions across domain/contracts, API, mobile, fixtures, and tests; inspect the existing dirty diff before allocating it to a slice. USDT-01's reviewed 653-line boundary is excluded. |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | Adapter/contract → P0 account/write path → P0 transfer/dashboard → P1 budgets → P1 recurrence → P1 reports → mobile selection/commands → mobile views/journey; split any over-budget unit once along a cohesive behavior boundary, not by stripping tests. |
| Delivery strategy | auto-chain |
| Chain strategy | stacked-to-main (user-confirmed for this session; no branch, commit, or PR authorized in this batch) |

Decision needed before apply: No
Chained PRs recommended: Yes
Chain strategy: stacked-to-main
400-line budget risk: High

`auto-chain` and the user-confirmed `stacked-to-main` strategy resolve the delivery decision for apply. Each future PR follows the prior merged slice into main; no branch, commit, PR, or remote action is authorized by this plan or this baseline batch. The historical USDT-01 exception and the ODD tracker’s older `ask-on-risk` value do not change this session's 400-line policy. Each unit below is a candidate review slice, not a promise that its measured diff is below 400: make one honest cohesive slicing pass, then report any irreducible overage for maintainer approval. Keep behavior and its tests together in each slice.

### Suggested Work Units

Commands are planned, not executed. Run commands from the repository root; `pnpm --dir` resolves the project directory. Any local database/API or device/browser harness requires an explicitly approved disposable local target; unavailable harness evidence stays unexecuted, never passed. For a feature-branch-chain, if selected, PR 1 bases on the feature/tracker branch and each later PR bases on the immediately previous PR branch; retarget polluted child diffs. For stacked-to-main, each PR follows the prior merged slice into main. Do not create PRs without separate authorization.

| Unit | Goal / autonomous finish | Likely PR | Focused test command | Runtime harness | Rollback boundary |
|---|---|---|---|---|---|
| 0 (completed baseline) | USDT-01 at `5fc5de4`: catalog, four-character storage, contracts; regression only, no rewrite | Already reviewed | `pnpm --dir packages/domain test` and `pnpm --dir packages/contracts typecheck` | N/A: no new runtime behavior in this plan; existing migration is immutable | Never roll back the widening migration or committed catalog to undo later slices. |
| 1 | Preserve dirty work, canonical adapter, request guards and additive dashboard flow contract; prove input/rate boundary | PR 1 | `pnpm --dir apps/api exec node --test --experimental-strip-types test/usdt-monetary.spec.mjs` | In-process API monetary adapter with real domain Money/FxRate and Prisma Decimal in `test/usdt-monetary.spec.mjs` | Only new adapter/guard/contract/test edits; preserve pre-existing dirty domain files and untracked module. |
| 2 | Exact account/onboarding/transaction posting and immutable directional FX evidence | PR 2 | `pnpm --dir apps/api exec node --test --experimental-strip-types test/accounts.spec.mjs test/usdt-monetary.spec.mjs` | In-process account/onboarding/transaction service fixture with reload and denied-write assertions | Account/write/evidence behavior and tests; keep foundation, pre-existing unrelated edits and existing USDT records readable. |
| 3 | Atomic transfers plus full-period dashboard balances/flow; guarded P0 registration | PR 3 | `pnpm --dir apps/api exec node --test --experimental-strip-types test/usdt-monetary.spec.mjs test/route-matrix.spec.mjs` | Staged transaction fixture injects failure after one posting and during snapshot creation; asserts rollback | Transfer/dashboard/wiring edits and matching tests; do not delete the pre-existing untracked module or change transfer fee policy. |
| 4 | P1 budget extraction with six-decimal partial progress and ownership | PR 4 | `pnpm --dir apps/api exec node --test --experimental-strip-types test/usdt-planning.spec.mjs test/month-bounds.spec.mjs test/p1-route-matrix.spec.mjs` | In-process budget service fixture with native and stored-snapshot expenses, unmatched transaction ID | Budget service, month adapter, wiring, and their tests; preserve public routes and existing budgets. |
| 5 | P1 recurring-rule extraction, exact occurrences and serializable catch-up | PR 5 | `pnpm --dir apps/api exec node --test --experimental-strip-types test/usdt-planning.spec.mjs test/p1-route-matrix.spec.mjs` | Catch-up twice through same time; assert two unique occurrences and `0.999998` native balance | Recurrence service/wiring/tests only; retain scheduler, unique key and existing rule data. |
| 6 | P1 historical report extraction and canonical totals without repricing | PR 6 | `pnpm --dir apps/api exec node --test --experimental-strip-types test/usdt-planning.spec.mjs test/p1-route-matrix.spec.mjs` | In-process report fixture with missing/matching historical evidence and later changed rate | Report service/wiring/tests; retain existing report filters and transfer treatment. |
| 7 | Mobile dependencies, response decoders, pure formatting/commands, unit selection and entry | PR 7 | `pnpm --dir apps/mobile exec node --test --experimental-strip-types test/usdt-journey.spec.mjs` | Mocked fetch commands exercise unchanged retry key, failed write, successful write and refresh failure separately | Mobile dependency/decoder/command/form edits and tests; API and USDT-01 unaffected. |
| 8 | Onboarding success transition, native/dashboard/report presentation and integrated manual journey | PR 8 | `pnpm --dir apps/mobile test` and `pnpm --dir apps/mobile smoke` | On approved local API/database, `pnpm --dir apps/mobile start` then manually select USDT, create account, record expense, reload and inspect partial/complete states; otherwise report unavailable | Mobile screens/navigation/presentation/test edits only; keep server-side support and persisted USDT records readable. |

The pre-existing dirty tracked diff already counts 623 changed lines before the untracked P0 module, so do not represent the baseline-to-current diff as a clean <=400 PR. First classify author-owned versus unrelated changes and measure each proposed review boundary; preserve `.github/workflows/ci.yml`, `odd/tasks/usdt-monetary-unit.md`, and the untracked `apps/api/src/modules/p0-monetary.ts` in place. A work unit starts at the preceding verified boundary and finishes with its own behavior, tests, exact command results and rollback evidence. Keep source files normally below 500 lines and never over 800; no `any`, duplicated unit catalog, new migration, or binary floating-point money.

## Phase 0: Completed foundation and continuation boundary

- [x] 0.1 USDT-01 already committed/reviewed at `5fc5de4`: supported USD/EUR/VES/GBP/USDT identities, active precision metadata, shared contracts, four-character schema and idempotent USDT catalog seed. This is a baseline, not an implementation task; do not edit the committed migration.
- [x] 0.2 Before any product edit, inspect `apps/api/src/modules/accounts.ts` (read-only), `apps/api/src/modules/p0-finance.ts` (read-only), `apps/api/src/modules/p0.module.ts` (read-only), `apps/api/src/modules/p0-monetary.ts` (read-only), `packages/domain/src/monetary-unit.ts` (read-only), `packages/domain/src/index.ts` (read-only), `packages/domain/test/monetary-unit.test.ts` (read-only), `odd/tasks/usdt-monetary-unit.md` (read-only), and `.github/workflows/ci.yml` (read-only) against the current diff; record which edits predate this plan and preserve all user work. Verification: `git status --short` and `git diff --stat` show the initial baseline; do not reset or stage it.
- [x] 0.3 Run baseline regression against `packages/domain/test/monetary-unit.test.ts` (read-only), `packages/contracts/src/money.ts` (read-only), `apps/api/prisma/schema.prisma` (read-only), and `apps/api/prisma/migrations/20260922190000_usdt_monetary_unit/migration.sql` (read-only): `pnpm --dir packages/domain typecheck`, `pnpm --dir packages/domain test`, `pnpm --dir packages/contracts typecheck`, `pnpm --dir apps/api exec prisma validate --schema prisma/schema.prisma`, `pnpm --dir apps/api exec prisma generate --schema prisma/schema.prisma`. Confirm all nine foundation scenarios (syntax, distinct identity, unsupported ABCD, active precision, fiat compatibility, USDT/fiat round trips, idempotent seed, manual semantics and no implicit parity) without reimplementing USDT-01. Prisma validate/generate are not migration execution evidence.

### Baseline batch evidence (tasks 0.2–0.3)

- Task 0.2: `git status --short` identified eight pre-existing tracked edits, untracked `apps/api/src/modules/p0-monetary.ts`, and untracked `openspec/`; `git diff --stat` reported eight tracked files, 202 insertions and 430 deletions before this artifact update. Inspected each named tracked diff plus the untracked monetary module read-only. API/domain edits predate this batch and remain unverified; ODD history and unrelated CI setup-order edit remain untouched. No files were staged or reset.
- Task 0.3 earlier state (superseded by the authorized completion below): `pnpm --dir packages/domain typecheck` (exit 0), `pnpm --dir packages/domain test` (72/72, exit 0), `pnpm --dir packages/contracts typecheck` (exit 0), and `pnpm --dir apps/api exec node --test test/schema.spec.mjs` (3/3 structural checks, exit 0). Initial Prisma attempts failed during config loading because `apps/api/prisma.config.ts` uses `env('DATABASE_URL')`. Later `pnpm --dir apps/api exec prisma validate --schema prisma/schema.prisma` and `pnpm --dir apps/api exec prisma generate --schema prisma/schema.prisma` both passed with a disposable synthetic loopback URL set only in the command process. These commands did not connect to or mutate a database. At that time the nine foundation scenarios were not fully verified, and task 0.3 correctly remained unchecked until a disposable database was authorized.

| Task 0.3 work unit evidence | Exact result / boundary |
|---|---|
| Focused tests | Earlier `pnpm --dir packages/domain test`: 72/72 passed, exit 0; `pnpm --dir apps/api exec node --test test/schema.spec.mjs`: 3/3 structural checks passed, exit 0. Retry: Prisma validate passed, exit 0; Prisma generate passed, exit 0 (client v7.10.0). |
| Runtime harness | Superseded by the authorized local PostgreSQL 18.3 run recorded below; earlier attempts had no approved database target. |
| Rollback boundary | Revert only this task 0.3 evidence update; leave task 0.2, baseline product files, the committed migration, the untracked module, ODD tracker, and CI unchanged. |

- Earlier work unit evidence (superseded for task 0.3): focused check `pnpm --dir packages/domain test` passed 72/72 and schema structural check passed 3/3. Runtime harness: N/A for task 0.2 (read-only baseline); at that point task 0.3 database-backed checks were unexecuted. Rollback boundary for that earlier batch: revert only its chain-strategy and baseline-evidence lines, preserving pre-existing source, test, ODD, CI, migration, and untracked module bytes. No PR boundary exists for this batch.

### Task 0.3 authorized disposable-database completion (2026-09-23)

- Focused tests: `pnpm --dir packages/domain typecheck` exit 0; `pnpm --dir packages/domain test` 72/72 passed, exit 0 (syntax, unsupported `ABCD`, distinct active USDT/fiat metadata and exact domain monetary behavior); `pnpm --dir packages/contracts typecheck` exit 0; `pnpm --dir apps/api exec node --test test/schema.spec.mjs` 3/3 passed, exit 0. With a process-scoped, redacted disposable `DATABASE_URL`, `pnpm --dir apps/api exec prisma validate --schema prisma/schema.prisma` and `pnpm --dir apps/api exec prisma generate --schema prisma/schema.prisma` both exited 0; generated Prisma Client v7.10.0.
- Runtime harness: initialized a new uniquely named PostgreSQL **18.3** cluster under the approved temporary parent with SCRAM authentication, a disposable user, a free high port bound only to `127.0.0.1`, and a separate empty database. Checked server data directory, address, port, user and database identity **before** `pnpm --dir apps/api exec prisma migrate deploy` (exit 0: all three migrations applied, including the USDT widening/catalog migration). Bounded SQL fixture writes and post-commit reads from another connection passed for USDT references/`1.234567` amounts in account, preferences, transactions, FX rate and snapshot, transfer, budget and recurring rule; fiat account `1.20`/USD base survived. A signed ledger check returned `1.234568` from `1.234567 + 0.000002 - 0.000001`. An account was created/read manually without any wallet/network identity. USDT and USD retained distinct codes, and there was no implicit USDT/USD rate. Repeating the catalog's `INSERT ... ON CONFLICT DO UPDATE` statement twice left exactly one active USDT row at precision six. `prisma.config.ts` has **no configured seed command**: this tests the committed migration's idempotent catalog upsert, not an invented `prisma db seed` execution. Prisma validate/generate alone are not counted as migration evidence; the separate deploy is.
- The first isolated attempts failed before writes due to a harness identity parse error and then a PostgreSQL `inet` address formatting mismatch; a later fixture initially failed on missing Prisma-managed `updatedAt` values in raw SQL. Each attempt stopped and removed only its own cluster. The corrected run passed. PostgreSQL 18.3 is **not** the repository's PostgreSQL 17 target; repeat database integration against 17 when available. No API service or mobile journey behavior is certified by this foundation-only check.
- Rollback boundary: revert only this task 0.3 checkbox and completion-evidence subsection, preserving the pre-existing baseline evidence, all source/test/CI/ODD changes, untracked P0 module, and committed migration. All disposable clusters, temporary logs and credential files were stopped/removed; the process-scoped URL and password were cleared. No commit, PR, remote operation, or new migration was made.

## Phase 1: Canonical precision adapter and boundary tests (Unit 1)

- [x] 1.1 Add behavioral cases to `apps/api/test/usdt-monetary.spec.mjs` for accepted USDT `1.234567`, zero `0.000000`, redundant trailing zeros, numeric/null/array/object/lowercase/`ABCD` code and numeric amount rejection, positive sub-unit rejection, and Decimal overflow/rate-scale rejection; include real Prisma Decimal `0.0000001` plain-string serialization and already-stored high-scale read without rewriting history. Fail the cases before changing the adapter; `pnpm --dir apps/api exec node --test --experimental-strip-types test/usdt-monetary.spec.mjs` is the focused command.
- [x] 1.2 Reconcile `packages/domain/src/monetary-unit.ts`, `packages/domain/src/index.ts`, and `packages/domain/test/monetary-unit.test.ts` without replacing dirty hardening: export only needed existing exact decimal primitives and test unsupported/non-string lookups; extend `packages/domain/test/money.test.ts` and `packages/domain/test/fx-rate.test.ts` for HALF_UP ties away from zero, `1.234567 USDT × 1.005 = 1.24 USD`, `× 36.5 = 45.06 VES`, `1.00 USD × 1.2345675 = 1.234568 USDT`, signed `+/-1 × 1.005 = +/-1.01 USD`, zero conversion and reverse-direction failure. Verify domain test/typecheck.
- [x] 1.3 Add `apps/api/src/common/request-input.ts` with reusable `unknown` object/string narrowing, and reconcile `apps/api/src/modules/p0-finance.ts` into a thin Money/FxRate/Prisma.Decimal adapter. Validate native strings once at unit precision and Decimal(20,8)/rate Decimal(24,12) capacity; use lossless accepted text for both storage and response, preserve full positive rate scale, target-round computed money only, serialize trusted Decimal without exponent or `String(unknown)`. Verify task 1.1 cases and `pnpm --dir apps/api typecheck`.
- [x] 1.4 Add `partial` and `warnings` to the existing flow shape in `packages/contracts/src/dashboard.ts` (not a mobile-local DTO); verify `pnpm --dir packages/contracts typecheck` and API typecheck before consuming the additive fields.

### Unit 1 work-unit evidence (2026-09-23)

| Evidence | Observed result |
|---|---|
| Focused test command and exact result | `pnpm --dir apps/api exec node --test --experimental-strip-types test/usdt-monetary.spec.mjs`: RED 1/5 passed, 4/5 failed before adapter implementation (missing nativeAmount, decimalText, convertAmount); GREEN 6/6 passed after implementation. `pnpm --dir packages/domain test`: 74/74 passed. |
| Runtime harness command/scenario and exact result | The focused node:test command imports the real domain Money/FxRate and Prisma.Decimal and executes adapter boundary/conversion scenarios in-process: 6/6 passed. No database operation was performed. |
| Rollback boundary | Undo only Unit 1's adapter, request-input, domain export/test additions, dashboard contract, API TypeScript config, and focused test changes. Restore only Unit 1's edits to pre-existing dirty accounts, P0 module, and untracked P0 monetary file by inspected hunk; do not reset those files or remove the untracked module. |

Unit 1 checks: `pnpm --dir packages/domain typecheck` exit 0; `pnpm --dir packages/domain test` 74/74 passed; `pnpm --dir packages/contracts typecheck` exit 0; `pnpm --dir apps/api typecheck` exit 0; focused API harness 6/6 passed; `git diff --check` exit 0 (line-ending warnings). The broader `pnpm --dir apps/api test` failed 32/33: the pre-existing `route-matrix.spec.mjs` counts guard text in `p0.module.ts` despite the already-dirty controller extraction into `p0-monetary.ts`. Task 2.6 owns the registration-test correction; this failed broad suite is not reported as a pass or used to certify P0 flows. Unit 1 adapter APIs exist but native write callers have not yet been migrated; that is Unit 2.

## Phase 2: P0 account/write path, FX and safe registration (Units 2–3)

- [x] 2.1 Extend `apps/api/test/accounts.spec.mjs` and `apps/api/test/usdt-monetary.spec.mjs` with executable account/onboarding/transaction fixtures: opening `1.234567` plus `0.000002` income minus `0.000001` expense reloads `1.234568`; onboarding with USDT base/account and zero reads `0.000000`; same-unit needs no rate; archive and uncategorized cross-unit restrictions, invalid inputs and foreign-account requests leave no account/transaction/ledger/balance mutation. Establish failures before changing the affected service; run the Unit 2 focused command.
- [x] 2.2 Reconcile `apps/api/src/modules/accounts.ts` and the existing untracked `apps/api/src/modules/p0-monetary.ts` in place (never replace or delete the latter): validate active supported string codes and accepted native amount exactly once, persist normalized strings for posting and DTO, preserve ownership, archive/category guards and USDT zero onboarding. Verify task 2.1 against reloaded rows and fiat compatibility.
- [x] 2.3 Extend `apps/api/test/usdt-monetary.spec.mjs` with RED evidence-selection cases: exclude future/reverse-only rates; reject zero/negative manual rates; retain selected MARKET or MANUAL source, direction, effective time, rate and money after later rate insertion; missing historical USDT/USD yields `MISSING_FX_RATE`/422 and no financial mutation. Implement `apps/api/src/modules/fx-evidence.ts` as a direct-pair historical selector returning selected evidence or explicit missing; wire it into `apps/api/src/modules/accounts.ts` and `apps/api/src/modules/p0-monetary.ts`, preserving existing transaction boundaries. Verify no implicit USDT/USD parity or historical repricing.
- [x] 2.4 Add `apps/api/test/fixtures/monetary-harness.mjs` as a staged in-memory transaction/rollback fixture and RED `apps/api/test/usdt-monetary.spec.mjs` cases for same-unit `0.000001` USDT transfer, cross-unit `1.234567` USDT → `45.06` VES at 36.5, sequential replay with unchanged key, later-rate immutability, missing-rate no-write, and injected failure after first posting **and** during snapshot creation. Then finish transfer logic in `apps/api/src/modules/p0-monetary.ts`; verify both account balances and all transfer/ledger/snapshot rows roll back, and transfer principal remains excluded from dashboard flow. Do not claim concurrent/crash-safe idempotency.
- [x] 2.5 Add `apps/api/test/usdt-monetary.spec.mjs` executable dashboard cases for USD-base `10.00` USD + `2.123456` USDT with no rate (known subtotal, partial, pair warning, native balances), future-only historical rate (flow partial and native activity), >50 transactions contributing to full-period flow while only activity is capped, complete USDT income `1.234567`/expense `0.000001`, and empty `0.000000` totals without warnings. Finish the dashboard read path in `apps/api/src/modules/p0-monetary.ts` and extract `apps/api/src/modules/dashboard.ts` if it would approach 500 lines; separate balance and flow warning collections, retain existing transfer exclusion/fee treatment. Verify the Unit 3 focused command.
- [x] 2.6 **Threat RED — P0 registration:** extend `apps/api/test/route-matrix.spec.mjs` so a missing provider after extraction or an unguarded moved controller fails; assert existing `/v1` routes, guards and exactly one controller registration instead of obsolete single-file source counts. Then reconcile `apps/api/src/modules/p0.module.ts` to register/re-export the extracted controllers/providers with their guards unchanged. Focused command: `pnpm --dir apps/api exec node --test --experimental-strip-types test/route-matrix.spec.mjs`.
- [x] 2.7 **Threat RED — P0 ownership:** extend `apps/api/test/usdt-monetary.spec.mjs` to attempt another user's account and category; assert denial precedes transaction/transfer/snapshot/ledger/balance writes. Then narrow request and ownership boundaries in `apps/api/src/modules/accounts.ts` and `apps/api/src/modules/p0-monetary.ts` using `apps/api/src/common/request-input.ts`; verify denied writes and Unit 2/3 regression tests.

### Task 2.7 P0 ownership boundary evidence (2026-09-24)

| Work Unit Evidence | Observed result |
|---|---|
| Focused test command and exact result | Threat RED: `pnpm --dir apps/api exec node --test --experimental-strip-types test/usdt-monetary.spec.mjs test/route-matrix.spec.mjs` failed 2/28 (26 passed): both ownership-change-before-posting tests accepted unauthorized writes. GREEN: `pnpm --dir apps/api exec node --test --experimental-strip-types test/accounts.spec.mjs test/usdt-monetary.spec.mjs test/route-matrix.spec.mjs` passed 48/48, exit 0, including direct foreign account/category, stale ownership, account update/archive scoping, Unit 2/3 and registration regressions. |
| Runtime harness command/scenario and exact result | The GREEN command runs real AccountsService and TransfersService against in-process Prisma-shaped, rollback-staged fixtures. Revoking source/destination account or category ownership after preflight but before posting rejects with NOT_FOUND before any transaction, transfer, snapshot, ledger, or balance mutation; direct foreign IDs also reject. 48/48 passed. Not a live PostgreSQL or HTTP concurrency test; no database target was authorized. |
| Rollback boundary | Revert only task 2.7's scoped ownership/name hunks in dirty `apps/api/src/modules/accounts.ts` and previously untracked `apps/api/src/modules/p0-monetary.ts`, appended ownership tests in dirty `apps/api/test/accounts.spec.mjs` and previously untracked `apps/api/test/usdt-monetary.spec.mjs`, staged ownership hook in `apps/api/test/fixtures/monetary-harness.mjs`, and this checkbox/evidence. Preserve all earlier contents in untracked files, tasks 2.1–2.6, domain/contracts, CI, ODD, schema and migrations. |

Required checks: focused 48/48 exit 0; `pnpm --dir apps/api typecheck` exit 0; `pnpm --dir apps/api test` 58/58 exit 0; `pnpm --dir packages/domain typecheck` exit 0; `pnpm --dir packages/domain test` 74/74 exit 0; `pnpm --dir packages/contracts typecheck` exit 0; `git diff --check` exit 0 with pre-existing CRLF warnings. The test module is 485 lines, `accounts.ts` remains below 500, and `p0-monetary.ts` remains below 500. Authored task additions are approximately 120 lines; the baseline Git diff cannot isolate this slice because several files were already dirty/untracked. Docs consulted: official Prisma ORM v7 transactions page for callback rollback and scoped queries; NestJS 12.0.1 installed version and Context7 source for existing controller/guard patterns. No commit, branch, PR, remote, or database action was performed. This is a stacked-to-main candidate slice following earlier verified P0 work; PR creation needs separate authorization.

### Unit 2 bounded account/onboarding/transaction evidence (tasks 2.1–2.2, 2026-09-23)

| Evidence | Observed result |
|---|---|
| Focused test command and exact result | `pnpm --dir apps/api exec node --test --experimental-strip-types test/accounts.spec.mjs test/usdt-monetary.spec.mjs`: RED 22/24 passed, 2/24 failed before service changes (invalid opening accepted; onboarding persisted `0` instead of `0.000000`); GREEN 24/24 passed after changes. |
| Runtime harness command/scenario and exact result | The focused command executes actual `AccountsService` and `OnboardingService` with in-process persisted fixture rows and reloads: `1.234567 + 0.000002 - 0.000001 = 1.234568` USDT, zero onboarding `0.000000`, independent USD-base/USDT-account selection, fiat `1.20`, same-unit without FX, and denied invalid/archived/uncategorized/foreign requests with unchanged financial rows: 24/24 passed. The fixture is not a PostgreSQL integration test; no database access was approved. |
| Rollback boundary | Revert only this batch's `apps/api/test/accounts.spec.mjs`, additional onboarding test in `apps/api/test/usdt-monetary.spec.mjs`, and inspected hunks in `apps/api/src/modules/accounts.ts` and existing untracked `apps/api/src/modules/p0-monetary.ts`, plus these task checkboxes/evidence. Preserve all Unit 1 edits, task 2.6, existing untracked module content, CI/ODD/schema/migration, and all other dirty files. |

Additional checks: `pnpm --dir apps/api typecheck` exit 0; `pnpm --dir apps/api test` 37/37 passed; `pnpm --dir packages/domain typecheck` exit 0; `pnpm --dir packages/domain test` 74/74 passed; `pnpm --dir packages/contracts typecheck` exit 0; `git diff --check` exit 0 with existing working-copy CRLF warnings. No formatter check script is declared in the root/API manifests. Process Node reported 24.14.0, not the pinned 24.20.0; no installation was performed. This slice does not certify cross-unit FX evidence, transfers, dashboards, PostgreSQL behavior, or broad task 2.7 ownership coverage.

### Task 2.6 corrective registration evidence (2026-09-23)

| Evidence | Observed result |
|---|---|
| Focused RED and GREEN | Before test correction `pnpm --dir apps/api test` failed 32/33 on `route-matrix.spec.mjs:18` because the old five-guard text count inspected only `p0.module.ts`. After correction `pnpm --dir apps/api exec node --test --experimental-strip-types test/route-matrix.spec.mjs` passed 3/3 and `pnpm --dir apps/api test` passed 34/34. The executable negative cases assert that a missing extracted provider, duplicate controller, or removed controller guard fails the registration assertion. |
| Runtime harness | The focused Node test imports real NestJS 12 controller/provider arrays, AppModule decorator metadata, route HTTP/path metadata, and AuthGuard metadata in process using the existing tsx loader: 3/3 passed. No HTTP server or database was started; this does not verify ownership or financial writes. |
| Rollback boundary | Revert only the route-matrix test correction and this task 2.6 checkbox/evidence. Existing P0 provider/controller imports, exports, and guards were already correct; no production wiring was changed. Preserve the pre-existing untracked monetary module and Unit 1 evidence. |

`pnpm --dir apps/api typecheck` exit 0; `pnpm --dir packages/contracts typecheck` exit 0; `pnpm --dir packages/domain test` 74/74 passed; `git diff --check` exit 0 (working-tree line-ending warnings). This corrective slice does not mark 2.1–2.5, 2.7, or later phases complete. The full API suite is now green for the currently implemented code only, not USDT-02 acceptance.

### Task 2.3 direct historical FX evidence (2026-09-23)

| Evidence | Observed result |
|---|---|
| Focused test command and exact result | `pnpm --dir apps/api exec node --test --experimental-strip-types test/usdt-monetary.spec.mjs`: RED 9/11 passed, 2/11 failed on wrong historical `effectiveAt` and lost MANUAL row source; GREEN 11/11 passed. An initial RED fixture run failed on cloning Prisma Decimal objects; the test fixture was corrected to use stored-rate strings before capturing the behavioral RED result. |
| Runtime harness command/scenario and exact result | The focused Node command invokes actual `AccountsService` and `TransfersService` against a stateful in-process Prisma-shaped fixture: future/reverse-only rows and missing pair yield 422 before postings, selected MARKET/MANUAL row metadata and recorded amounts survive later insertion, manual input uses operation time, and invalid rates leave financial rows unchanged; 11/11 passed. This is not PostgreSQL integration or the atomic transfer rollback exercise reserved for task 2.4. |
| Rollback boundary | Revert only the 2.3 test additions, new `apps/api/src/modules/fx-evidence.ts`, and the small evidence-selection hunks in pre-existing dirty `accounts.ts` and pre-existing untracked `p0-monetary.ts`; preserve their other contents, earlier task evidence, migration, CI and ODD. |

Task 2.3 verification: focused harness 11/11, API typecheck exit 0, API tests 41/41, domain typecheck exit 0 and tests 74/74, contracts typecheck exit 0, `git diff --check` exit 0 with existing CRLF warnings. Installed Node 24.14.0 differs from pinned 24.20.0; pnpm 11.24.0 matches. No dependencies, database, branches, commits or PRs were changed. Measured test growth: 116 to 235 lines (+119); new resolver: 32 lines; account module: 280 to 266 (-14 net); pre-existing untracked P0 module: 423 to 406 (-17 net). These are file line counts, not an isolated authored additions-plus-deletions total: the latter cannot be extracted from Git without a pre-slice snapshot of the dirty/untracked files; do not attribute their full baseline diff to this task.

### Task 2.4 staged transfer evidence (2026-09-23)

| Work Unit Evidence | Observed result |
|---|---|
| Focused test command and exact result | `pnpm --dir apps/api exec node --test --experimental-strip-types test/usdt-monetary.spec.mjs`: behavioral RED 15/16 passed, 1/16 failed (unrepresentable source accepted) before production change; GREEN 17/17 passed after change and fee regression. The first attempt before RED did not load because the fixture was statically imported ahead of the tsx loader; moving it to a dynamic import fixed test setup. `pnpm --dir apps/api exec node --test --experimental-strip-types test/route-matrix.spec.mjs`: 3/3 passed. |
| Runtime harness command/scenario and exact result | The focused Node command executes actual TransfersService and DashboardService against a staged in-process transaction fixture: same-unit micro-postings, USDT→VES conversion, one unchanged-key replay, immutable transfer evidence after a later rate, missing direct rate with no writes, fee posting, dashboard principal exclusion, and injected failures after the first posting and during snapshot creation with unchanged balances and transfer/transaction/ledger/snapshot/snapshot-rate rows: 17/17 passed. No PostgreSQL/database integration or concurrent/crash-safe idempotency claim. |
| Rollback boundary | Revert only this task's 86-line `apps/api/test/fixtures/monetary-harness.mjs`, appended tests in the pre-existing untracked `apps/api/test/usdt-monetary.spec.mjs`, and source/fee normalization hunks in the pre-existing untracked `apps/api/src/modules/p0-monetary.ts`; preserve the earlier contents of both untracked files, previous task checkboxes/evidence, all other dirty user files, CI, ODD, schema and migration. |

Task 2.4 checks: `pnpm --dir apps/api typecheck` exit 0; `pnpm --dir apps/api test` 47/47 passed; `pnpm --dir packages/domain typecheck` exit 0 and `pnpm --dir packages/domain test` 74/74 passed; `pnpm --dir packages/contracts typecheck` exit 0; `git diff --check` exit 0 with existing CRLF warnings. Focused tests 17/17 and route matrix 3/3 passed. Handwritten file sizes: P0 monetary 405 lines, USDT test 332 lines, harness 86 lines. Added test/fixture text and a small source edit form one cohesive review slice, roughly 200 authored changed lines; exact isolated additions-plus-deletions are not recoverable from Git for the pre-existing untracked files without a pre-slice snapshot. Installed Node 24.14.0 differs from pinned 24.20.0; pnpm 11.24.0 matches. Prisma 7.10.0 interactive transaction rollback behavior was checked against the official Prisma ORM v7 transactions documentation. No database, schema, dependency, remote, branch, commit or PR change was made.

### Task 2.5 dashboard evidence (2026-09-24)

| Work Unit Evidence | Observed result |
|---|---|
| Focused test command and exact result | `pnpm --dir apps/api exec node --test --experimental-strip-types test/usdt-monetary.spec.mjs test/route-matrix.spec.mjs`: RED 23/25 passed, 2/25 failed (flow warning leaked into balances; period flow omitted transactions beyond the first 50). GREEN 26/26 passed after implementing separate warning sets and independent full-period flow/activity queries; the sixth dashboard case verifies historical rate selection versus a stored snapshot. |
| Runtime harness command/scenario and exact result | The focused command executes the actual DashboardService and registered NestJS P0 route metadata with in-process Prisma-shaped data: native mixed-unit balances, simultaneous missing balance/flow evidence, future/reverse-only flow rates, historical eligible rate and immutable snapshot preference, 55 income transactions plus excluded transfer principal with activity limited to 50, complete six-decimal USDT totals and empty zeros: 26/26 passed. No database, HTTP server, or API integration was executed. |
| Rollback boundary | Revert only this task's dashboard tests in the existing untracked `apps/api/test/usdt-monetary.spec.mjs`, the appended dashboard fixture in `apps/api/test/fixtures/monetary-harness.mjs`, the full-period query/warning changes in the pre-existing untracked `apps/api/src/modules/p0-monetary.ts`, and this checkbox/evidence. Retain earlier content of every untracked file, earlier tasks, transfer fee/principal policy, foundation, CI, and ODD. |

Task 2.5 checks: focused 26/26 passed; `pnpm --dir apps/api typecheck` exit 0; `pnpm --dir apps/api test` 53/53 passed; `pnpm --dir packages/domain typecheck` exit 0 and `pnpm --dir packages/domain test` 74/74 passed; `pnpm --dir packages/contracts typecheck` exit 0; `git diff --check` exit 0 with existing working-copy CRLF warnings. Dashboard stayed below 500 lines in the existing P0 module, so no extraction was needed. Prisma Client 7.10.0 `findMany` filtering and `take` semantics were checked against official Prisma ORM v7 filtering/pagination documentation; the public NestJS providers/modules pages did not render substantive version-specific content, so existing installed NestJS 12.0.1 registration patterns and the executable route-matrix test were retained. No PR, branch, commit, database, or remote action was performed.

## Phase 3: Focused P1 extraction and historical totals (Units 4–6)

- [x] 3.1 **Threat RED — P1 registration:** extend `apps/api/test/p1-route-matrix.spec.mjs` to fail if an extracted provider is missing, a moved controller loses its guard, `/v1` routes duplicate, or a foreign category/rule is accepted; add runtime denied-ownership/no-financial-write cases to `apps/api/test/usdt-planning.spec.mjs`. Only then change `apps/api/src/modules/p1.module.ts` registrations/guards and extracted services. Verify both focused suites; never rely on source text alone for ownership.
- [x] 3.2 Add RED budget cases in `apps/api/test/usdt-planning.spec.mjs`: limit `1.000001`, native expense `0.000002`, remaining `0.999999`; unmatched USD expense yields partial USD/USDT warning and affected ID without an invented conversion; matching immutable snapshot contributes despite a later rate; negative over-budget remaining, stored budget unit after preference change, month/timezone/void/category filters, invalid input/foreign budget no write. Preserve existing `apps/api/test/month-bounds.spec.mjs` assertions.
- [x] 3.3 Extract the existing month bound implementation into `apps/api/src/modules/planning-time.ts`, re-export `validatedMonthBounds` from `apps/api/src/modules/p1.module.ts`, and extract typed `BudgetsService` into `apps/api/src/modules/budgets.ts`. Reuse canonical metadata/adapter instead of three-letter validation, local defaults or `fixed(..., 2)`; use stored budget code for updates and snapshots only for cross-unit history. Retain controllers/DTO routes in `p1.module.ts`; pass task 3.2 tests and API typecheck.
- [ ] 3.4 Add RED recurrence cases in `apps/api/test/usdt-planning.spec.mjs` for two due `0.000001` USDT expenses from `1.000000` (exactly two transactions, `0.999998`, unchanged listing amount), repeated catch-up creates none, currency-mismatch USD rule for USDT account causes no rule/ledger write, foreign rule rejection, and precision/sign in postings.
- [ ] 3.5 Extract typed `RecurringRulesService` to `apps/api/src/modules/recurring-rules.ts`; retain cadence, 500-occurrence bound, serializable catch-up, `(ruleId, occurrenceAt)` uniqueness and account currency matching. Re-export/register from `apps/api/src/modules/p1.module.ts`; verify task 3.4 and P1 route/ownership tests without changing scheduling behavior.
- [ ] 3.6 Add RED report cases in `apps/api/test/usdt-planning.spec.mjs` for USD income `10.00` plus USDT expense `-0.000001` without matching snapshot (native totals, partial warnings/IDs, omitted `baseTotal`); complete USDT `1.234567 - 0.000001 = 1.234566`, empty base totals, matching stored snapshot unaffected by new prices, half-open range and existing filters/transfer treatment.
- [ ] 3.7 Extract typed `ReportsService` to `apps/api/src/modules/reports.ts` and register/re-export it through `apps/api/src/modules/p1.module.ts`; format native totals per unit, emit base total only when complete, use matching snapshots rather than current rates. Remove P1 duplicate `currencyDefaults`/`code`/`add`/`multiply`/`fixed` and handwritten `any` from touched P1 paths, including controllers/category guards; keep modules under the source limits. If DTO imports require it, declare `@lukitas/contracts` in `apps/api/package.json`. Verify task 3.6 and `pnpm --dir apps/api typecheck`/`pnpm --dir apps/api test`.

### Unit 4 P1 budget evidence (tasks 3.1–3.3, 2026-09-24)

| Work Unit Evidence | Observed result |
|---|---|
| Focused test command and exact result | Threat/budget RED: `pnpm --dir apps/api exec node --test --experimental-strip-types test/usdt-planning.spec.mjs test/p1-route-matrix.spec.mjs`: 3/9 passed, 6/9 failed on precision, validation and hostile ownership fixtures before source changes. GREEN: `pnpm --dir apps/api exec node --test --experimental-strip-types test/usdt-planning.spec.mjs test/month-bounds.spec.mjs test/p1-route-matrix.spec.mjs`: 13/13 passed, exit 0. Route negative probes throw for missing provider, duplicate controller and removed guard. |
| Runtime harness command/scenario and exact result | Same focused Node command executes BudgetsService, RecurringRulesService denied ownership and NestJS route/guard metadata with in-process Prisma-shaped Decimal fixtures: six-decimal native/snapshot arithmetic, no current-rate lookup, affected ID/warning, foreign rule/category/budget denial before writes, half-open New York month; 13/13 passed. No database, HTTP listener or concurrent-write integration was authorized. |
| Rollback boundary | Revert only this unit's new `apps/api/src/modules/budgets.ts`, `planning-time.ts`, `apps/api/test/usdt-planning.spec.mjs`, edited `p1-route-matrix.spec.mjs`, inspected P1 import/extraction/typing/ownership hunks and these three checkboxes/evidence. Preserve earlier dirty/untracked P0 content and unrelated CI/ODD/schema/migrations. |

Checks: API typecheck exit 0; API tests 66/66; P0 focused regressions 48/48; domain typecheck exit 0 and tests 74/74; contracts typecheck exit 0; `git diff --check` exit 0 (existing CRLF warnings). NestJS 12.0.1 provider/guard registration checked against official versioned source and runtime metadata; Prisma Client 7.10.0 Decimal and filters checked against official v7 documentation and generated types. `p1.module.ts` remains 536 lines (below the 800 hard ceiling; recurrence/report extractions are later tasks). Tracked P1/route changes: 53+152 and 44+11 lines; new modules: 107+11 lines; planning test approximately 122 lines. Approximately 500 authored additions+deletions exceed the 400-line review budget. This is one cohesive budget/ownership/registration unit with its tests; recommend maintainer `size:exception` for any future PR rather than compressing tests or splitting the shared security boundary. Git baseline includes unrelated dirty/untracked content; no database access, branch, commit or PR.

## Phase 4: Mobile manual selection, submissions and presentation (Units 7–8)

- [ ] 4.1 Add existing workspace `@lukitas/domain` and `@lukitas/contracts` dependencies to `apps/mobile/package.json` and regenerate `pnpm-lock.yaml` only for those references; do not install a new external library or change SDK versions. Typecheck `apps/mobile` after linking.
- [ ] 4.2 Add RED pure tests in `apps/mobile/test/usdt-journey.spec.mjs` for supported active choices (USD/EUR/VES/GBP/USDT, no inactive/unsupported), independently selected USD base/USDT account and USDT base/USDT zero account, string-preserving account/transaction amounts, `9007199254740993.123456`, `0.000001`, `0.000000` USDT and `1.20` USD exact display; no network/price/custody fields. Then implement `apps/mobile/src/features/monetary.ts` using canonical domain metadata/Money and `apps/mobile/src/features/monetary-unit-select.tsx` using native controls. Large value is display-only; do not widen database storage.
- [ ] 4.3 Add RED `apps/mobile/test/usdt-journey.spec.mjs` cases for `unknown` financial responses, missing dashboard flow completeness fields treated as unavailable, omitted report `baseTotal` retained, and partial-to-complete refresh replacing stale warnings. Then implement `apps/mobile/src/api/financial-responses.ts` decoders and update `apps/mobile/src/api/client.ts` with real shared return/request DTOs and validated consumed JSON/errors, including correctly typed `report`. Verify mobile focused test and typecheck; no screen-local copied contracts or `any`.
- [ ] 4.4 **Threat RED — financial submission:** add mocked-fetch cases in `apps/mobile/test/usdt-journey.spec.mjs` for rejected validation/API response, pending duplicate submit, unchanged retry idempotency key, genuinely new command key, confirmed successful write, and write success followed by failed refresh; no rejected command appears successful, no optimistic balance, and retrying refresh must not duplicate a write. Then implement typed submission/refresh functions in `apps/mobile/src/features/monetary-commands.ts` and compose `apps/mobile/src/features/account-form.tsx` and `apps/mobile/src/features/transaction-form.tsx`; take transaction unit from the selected account. Verify Unit 7 focused test.
- [ ] 4.5 **Threat RED — onboarding navigation:** add callback/response rejection tests in `apps/mobile/test/usdt-journey.spec.mjs` and success-only callback wiring regression in `apps/mobile/test/p0-journey.spec.mjs`: failed/rejected setup stays on form, pending submit cannot double-send, and successful setup advances without a token change. Then update `apps/mobile/src/features/screens.tsx` and `apps/mobile/src/navigation/RootNavigator.tsx` to pass/consume `onComplete` only after confirmed success and refresh dashboard data; verify both tests and manually inspect transition in Unit 8 harness.
- [ ] 4.6 Update `apps/mobile/src/features/screens.tsx` to compose the selector/forms and display native USDT account/activity `1.234567`/`1.234566`, target-labeled `1.24` USD valuation, known-subtotal/flow partial labels and individual pair warnings; replace data atomically on refresh. Extend `apps/mobile/test/p0-journey.spec.mjs` for composition and `apps/mobile/test/usdt-journey.spec.mjs` for decoded partial-to-complete transitions; keep the screen module focused and below source limits.
- [ ] 4.7 Update `apps/mobile/src/features/reports-screens.tsx` to display every native total (including `-0.000001` USDT), pair warnings/affected IDs and unavailable (not zero) base total when omitted; test report decoding/presentation in `apps/mobile/test/usdt-journey.spec.mjs`. Verify USDT zero/sub-cent/large formatter and fiat formatting are unchanged across account, dashboard and report views.

## Phase 5: Evidence, rollback and acceptance

- [ ] 5.1 Verify each P0/P1 work unit before starting dependent mobile work: `pnpm --dir packages/domain typecheck`, `pnpm --dir packages/domain test`, `pnpm --dir packages/contracts typecheck`, `pnpm --dir apps/api typecheck`, `pnpm --dir apps/api test`, plus its focused node:test command. Keep behavior tests with each slice; record exact command, result, changed-line count and runtime harness result (or why unexecuted). Existing source-inspection route tests alone are not runtime acceptance.
- [ ] 5.2 Verify mobile independently with `pnpm --dir apps/mobile typecheck`, `pnpm --dir apps/mobile test`, and `pnpm --dir apps/mobile smoke`; perform the approved local manual USDT onboarding/account/expense/reload/partial-to-complete journey when a disposable local database/API and browser/device are available. Record real environment, result, failure/retry outcomes and limitations; do not infer successful interaction from export smoke or write to an unapproved database.
- [ ] 5.3 Run `pnpm --dir apps/api exec prisma validate --schema prisma/schema.prisma`, `pnpm --dir apps/api exec prisma generate --schema prisma/schema.prisma`, `pnpm test:all`, and `pnpm gate` after package checks. Explicitly report that `pnpm test:all` omits mobile tests and `pnpm gate` does not replace functional suites; distinguish unrun migration/database checks from actual passes. Inspect final changed-source diff for `any`, duplicate precision rules, files >=500 lines, unauthorized mutation of `apps/api/prisma/` (read-only), `odd/tasks/usdt-monetary-unit.md` (read-only) and `.github/workflows/ci.yml` (read-only), and preserve all pre-existing user work.
- [ ] 5.4 Before any delivery step, measure authored additions + deletions at each candidate PR boundary and confirm chain strategy with the team; if one honest cohesive split still exceeds 400 lines, report the smallest honest diff and request `size:exception` instead of compressing tests/docs. Record rollback per unit and preserve widened USDT reads during any later operational rollback; neither a commit nor a PR is part of this task-planning authorization.
