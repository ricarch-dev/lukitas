# Auth entry experience

## Objective
Let a new user create an account with email and password from the app, and replace the bare sign-in screen with a responsive, accessible entry experience inspired by the provided reference without copying its identity.

## Problem and why
The API already supports registration, but the mobile/web app exposes only sign-in and its current screen stretches edge to edge on desktop. Users must use a separate terminal to register.

## Scope and constraints
- Authorized: mobile authentication UI and its focused contracts/tests; reuse the existing `/auth/register`, `/auth/login`, session storage and navigation. Do not add a backend signup flow or dependencies unless evidence requires it.
- Preserve the already committed web session-storage fix from baseline `08780ac`; never include unrelated files in a work-unit commit.
- Design: distinct Lukitas identity, responsive web/native layout, visible sign-in/create-account switch, clear form states and keyboard/screen-reader labels. Avoid copying the reference logo, QR code, or exact palette.
- Artifact language: English for code, documentation, and task notes. AUTH-02 visible authentication copy: neutral/professional Spanish, as explicitly selected. Never include real credentials or personal data.
- TDD: Standard (strict TDD disabled), source: project testing context `sdd-init/lukitas` records `strict_tdd: false` without an affirmative enable setting. Runner: `pnpm --dir apps/mobile test`; also run mobile typecheck and applicable checks.
- Delivery: ask-on-risk. AUTH-02 is currently 1,006 changed lines (958 additions, 48 deletions), including source, tests, product/design context, and task evidence. Any PR carrying this candidate exceeds the 400-line budget; require chained/split PRs or maintainer-approved `size:exception` before PR creation. No size exception is approved and no remote operation is authorized.

## Work units
- [x] AUTH-01 — Reuse typed auth response/request contracts and wire registration into the session boundary with focused tests for success, invalid input, duplicate email and failure without storing tokens. Route: delegated direct; changes span multiple non-trivial files and reading prepares the write. Acceptance: typed registration action is ready for AUTH-02 to expose, saves valid tokens for normal onboarding; login stays intact. Checks: mobile 9/9, auth 5/5, domain 76/76, API 66/66; mobile/domain/API typechecks passed; parent reran mobile typecheck; diff whitespace passed. Runtime API call N/A: no disposable live API/database was started for this isolated auth helper; API behavior is covered by existing tests and integration remains to verify. Commit: `d19414d` (`feat(auth): add typed registration submission and tests`). RDD: approved and acknowledged (`review-b555f6508ecb65af`).
- [x] AUTH-02 — Replaced the bare auth view with a distinct, responsive sign-in/create-account UI and accessible form states; added mode/action and Spanish error-copy coverage without changing other screens. Route: delegated direct. Acceptance: desktop and narrow screens offer both modes, loading and known errors are understandable, password is obscured by default, and registration reuses the existing session/onboarding route. Changed paths: `PRODUCT.md`, `apps/mobile/src/features/auth-screen.tsx`, `apps/mobile/src/features/auth-screen-logic.ts`, `apps/mobile/src/features/auth-screen-theme.ts`, `apps/mobile/src/features/screens.tsx`, `apps/mobile/test/auth-screen.spec.ts`, `apps/mobile/test/p0-journey.spec.mjs`, `DESIGN.md`, `.impeccable/design.json`, and this task file. Checks: `pnpm --dir apps/mobile test` passed 15/15; `pnpm --dir apps/mobile typecheck` passed; `pnpm --dir apps/mobile smoke` passed (`mobile.smoke: pass`, with the existing Windows `DEP0190` warning); `git diff --check` passed. Web bundle export ran, but visual browser/native screenshot and device accessibility QA were unavailable. Commit: pending for parent. RDD: pending.

## Progress and verification
- AUTH-01 implemented and verified on `feat/auth-entry-experience`, branched from `08780ac1c8008bc12c408c356564b0e8a1fadf06`. Shared domain normalization matches the server; auth contract types remove the touched login path's `any`. Commit `d19414d`, 173 insertions / 11 deletions. Native 4-lens review approved and exact acknowledgement burned; no delivery action authorized or performed. The web session-storage fix is already committed in the baseline and unchanged.
- AUTH-02 implemented on `feat/auth-entry-experience` using the shared typed auth submit helper and existing onboarding navigation. The new Spanish screen includes responsive layout, mode selection, secure-by-default password reveal, accessible focus/loading/error feedback, and duplicate-email copy. Added six focused auth-screen tests and updated the P0 journey assertion for the extracted screen. No API/database, dependency, unrelated-screen, or storage-adapter changes. `DESIGN.md` documents the implemented auth visual system and `.impeccable/design.json` supplies its sidecar. Mobile tests passed 15/15, typecheck passed, web smoke export passed with the existing `DEP0190` warning, and `git diff --check` passed. No browser/native visual runtime or device accessibility pass was available. Commit and RDD review remain pending for the parent.
- Forecast: AUTH-01 commit is 184 changed lines; AUTH-02 is 1,006 changed lines. These remain separate review candidates, but the AUTH-02 PR slice still exceeds budget. Branch point: `08780ac1c8008bc12c408c356564b0e8a1fadf06`. Before opening a PR, choose a cohesive split/chain or obtain maintainer-approved `size:exception`; no PR strategy has been selected.

## Next step
Parent final review and commit AUTH-02 including root product context; perform browser/native visual and assistive-technology QA when a runtime/device harness is available. Preserve the already-committed baseline storage adapter.
